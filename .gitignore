# Dependencies / build output
node_modules/
dist/
.output/
.nuxt/
backend/generated/
figma/

# Secrets — NEVER commit
.env
.env.*
!.env.example

# Make.com blueprints contain real API keys — keep out of git
*.blueprint.json

# Legacy reference (rewrite/code.txt has hardcoded legacy secrets) — keep out of git
rewrite/

# Internal planning docs — for us, not for the repo
/REQUEST.md
/SPEC.md
/SETUP.md

# Tooling state
.omc/

# OS / editor
.DS_Store
Thumbs.db
.gstack/

# Виртуальное окружение
venv/
.venv/
env/

# Системные файлы macOS
.DS_Store

# Кэш Python
__pycache__/
*.pyc

# Файлы, которые генерирует сам скрипт (отчеты и дампы)
*.html
*.json

# Скрытые настройки Streamlit (чтобы не слить ключи, если они там есть)
.streamlit/
.env